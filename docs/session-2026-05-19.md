# Session Log — 2026-05-19

**Project:** Sistema web de gestión de nómina — Lubrimotos del Sur
**Developer:** Gabriel Iván Madrigal Flores

---

## What We Did

### 1. Repository setup

- Initialized a Git repository in the project folder (was not previously a git repo)
- Created `.gitignore` (excludes `.env`, `vendor/`, `logs/*.log`)
- Made the initial commit with all existing code (Auth stub, Dashboard, Puestos CRUD, schema, etc.)
- Pushed to GitHub: `https://github.com/Gabrixf/Proyecto_Graduacion_Gabriel_Madrigal_Flores`

### 2. Database setup

- Confirmed MySQL running via XAMPP (MariaDB 10.4.32)
- Imported `database/schema.sql` directly via `mysql.exe` CLI — created `lubrimotos_nomina` database with all 21 tables
- Created `.env` from `.env.example` (XAMPP defaults: root user, empty password)

### 3. Brainstormed Modules 1 & 2 (Auth + Mantenimientos)

Decisions made during brainstorming:

| Question | Decision |
|---|---|
| Scope | Auth + Mantenimientos together (Periodos, Feriados, Usuarios) |
| Architecture | Shared `UsuariosRepository` for both AuthService and UsuariosService |
| Audit logging | Auth + all Mantenimientos CRUD (not Puestos retroactively) |
| Login redirect | Both roles (admin + empleado) → `/dashboard` |
| Periodos validation | Quincenal enforced server-side (1–15 or 16–last day only) |
| Usuarios management | Full: create, edit, password reset, delete |

Design spec saved to: `docs/superpowers/specs/2026-05-19-auth-mantenimientos-design.md`

### 4. Implementation plan

15-task plan written with complete code for every file.

Plan saved to: `docs/superpowers/plans/2026-05-19-auth-mantenimientos.md`

### 5. Implemented — Auth + Mantenimientos (15 tasks via subagents)

| Task | What was built |
|---|---|
| 1 | Fixed `database/seed.sql` — replaced placeholder bcrypt hashes with real ones. Both seed users (`admin`, `jperez`) use password `password123`. |
| 2 | `AuditoriaRepository` — append-only audit log writer. Failures swallowed + logged via Monolog, never bubble up. |
| 3 | `UsuariosRepository` — shared by AuthService and UsuariosService. `findByNombreUsuario` returns hash; `findAll`/`findById` do not. |
| 4 | `AuthService` — bcrypt verification, user enumeration prevention, LOGIN/LOGOUT audit. |
| 5 | `AuthController` — replaced stub with real login. IP from `$_SERVER`, session set after success, audit before session destroy on logout. |
| 6 | `UsuariosService` — full CRUD. Self-lock guard (admin can't change own rol/activo). Blocks delete if user has linked empleado. |
| 7 | `UsuariosController` + 3 templates (index, form, password reset). Password field unset on re-render errors. |
| 8 | `PeriodosRepository` — overlap detection (`fecha_inicio <= :fin AND fecha_fin >= :inicio`), linked-records check across 5 tables. |
| 9 | `PeriodosService` — quincenal validation, estado transition guard (cerrado → abierto forbidden), next-period suggestion. |
| 10 | `PeriodosController` + 2 templates (index, form). Delete button only shown for open periods. Estado select only on edit. |
| 11 | `FeriadosRepository` — standard CRUD ordered by fecha. |
| 12 | `FeriadosService` — date format validation, mb_strlen check, duplicate date catch via SQLSTATE 23xxx. |
| 13 | `FeriadosController` + 2 templates (index, form). |
| 14 | Wired everything: replaced `config/dependencies.php` and `config/routes.php` with all new repositories, services, and controllers. Updated navbar links from `href="#"` to `path_for()`. |
| 15 | Smoke test: `composer install` (52 packages), autoloader verified, all php -l checks passed, seed data confirmed in DB. |

**Total commits added this session: 18**

### 6. Project moved from OneDrive to XAMPP htdocs

**Problem:** Project was in `C:\Users\Gmadr\OneDrive\Documentos\Proyecto_Graduacion` — OneDrive sync caused file-lock issues with Apache.

**Solution:**
- Copied project to `C:\xampp\htdocs\lubrimotos`
- Initialized fresh git repo, fetched from GitHub remote, reset to `main`
- Set `APP_BASE_PATH=/lubrimotos/public` in `.env`

**App URL:** `http://localhost/lubrimotos/public/`

**Original OneDrive folder** can be deleted — everything is in htdocs and on GitHub.

---

## Current Project State

```
C:\xampp\htdocs\lubrimotos\
├── .env                          ← APP_BASE_PATH=/lubrimotos/public
├── vendor/                       ← 52 packages installed
├── src/
│   ├── Controllers/              AuthController, DashboardController,
│   │                             PuestosController, UsuariosController,
│   │                             PeriodosController, FeriadosController
│   ├── Services/                 AuthService, PuestosService, UsuariosService,
│   │                             PeriodosService, FeriadosService
│   ├── Repositories/             AuditoriaRepository, UsuariosRepository,
│   │                             PuestosRepository, PeriodosRepository,
│   │                             FeriadosRepository
│   └── Middleware/               AuthMiddleware, RoleMiddleware
└── templates/
    ├── auth/, dashboard/, layouts/, partials/
    ├── puestos/, usuarios/, periodos/, feriados/
    └── (empleados, nominas, etc. — pending)
```

## Next modules (per agreed development order)

| # | Module | Branch |
|---|---|---|
| 3 | Gestionar Empleados + datos_bancarios | `feature/empleados` |
| 4 | Gestionar Asistencia | `feature/asistencia` |
| 5 | Gestionar Horas Extra | `feature/horas-extra` |
| 6–13 | Vacaciones → Reportes | `feature/*` |

## Login credentials (development)

| User | Password | Role |
|---|---|---|
| `admin` | `password123` | admin |
| `jperez` | `password123` | empleado |
